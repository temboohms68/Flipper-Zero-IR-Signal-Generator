App(
    appid="ir_signal_generator",
    name="IR Signal Generator",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="ir_signal_generator_app",

    requires=["gui"],
    stack_size=2 * 1024,
    order=20,
    fap_icon="icon.png",  # We will need an icon, for now placeholder
    fap_category="Infrared",
)
